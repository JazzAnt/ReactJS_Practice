// you can rename imports like so (but usually just keep the base name)
import pkmn from "pokemon"

console.log(pkmn.random())

//initialize npm using npm init
//import using npm install xxx
//look up npmjs.com for packages